# -----------------------------------------------------------------------------
# API KEYS AND CONFIGURATION FOR "JULES.GOOGLE"
# -----------------------------------------------------------------------------
# Copy this file to .env and fill in the values.
# NEVER SHARE OR COMMIT YOUR .env FILE!

# --- Google Cloud & Gemini API ---
# The full resource name of your GOOGLE_API_KEY secret in Google Secret Manager.
# The application will fetch the API key from here.
# e.g., "projects/your-gcp-project-id/secrets/your-google-api-key-secret-name/versions/latest"
GOOGLE_API_KEY_SECRET="your-google-api-key-secret-resource-name"

# --- Google Cloud Project ---
# Your Google Cloud Project ID.
GCP_PROJECT_ID="your-gcp-project-id"

# The region for your GCP resources (e.g., "us-central1").
GCP_REGION="your-gcp-region"

# --- Vertex AI Vector Search ---
# The full resource name of your Vector Search Index.
# e.g., "projects/your-gcp-project-id/locations/your-gcp-region/indexes/your-index-id"
VECTOR_SEARCH_INDEX_ID="your-vector-search-index-id"

# The full resource name of your Vector Search Index Endpoint.
# e.g., "projects/your-gcp-project-id/locations/your-gcp-region/indexEndpoints/your-endpoint-id"
VECTOR_SEARCH_ENDPOINT_ID="your-vector-search-endpoint-id"

# --- (Optional) Service Account ---
# Path to your Firebase/GCP service account key file (JSON).
# If you are running in a GCP environment (like Cloud Run) with a service account
# attached, you may not need this. For local development, this is required.
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-file.json"